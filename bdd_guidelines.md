# BDD Guidelines — AI 更新檢查清單

> **用途**：這是一份給 AI 助手的 SOP 檢查表。每次修改網站前後，必須逐條自我檢查。
>
> **適用範圍**：MythogenEngine 網站的所有修改（UI、內容、功能、設定）。
>
> **最後更新**：2026-07-15

---

## 強制檢查清單（每次修改必做）

### ✅ 1. 響應式版面檢查 (RWD)

- [ ] 修改是否影響了**手機版 (Mobile, ≤768px)** 的版面配置？
- [ ] 修改是否影響了**平板版 (Tablet, 769px-1024px)** 的版面配置？
- [ ] 修改是否影響了**電腦版 (Desktop, >1024px)** 的版面配置？
- [ ] 如果修改涉及 CSS，是否在 CSS Modules 內完成，沒有汙染全域 `custom.css`？
- [ ] 導覽列（navbar）在手機摺疊選單中是否仍正常？

### ✅ 2. 模組隔離檢查 (Isolation)

- [ ] 新增的功能是否**獨立運作**，沒有汙染現有主站的文章更新邏輯？
- [ ] 修改是否**沒有**改動 `sidebars.js` 中其他模組的結構？（除非本次任務明確要求）
- [ ] 修改是否**沒有**影響 `scripts/generateTechNotes.js` 或 `scripts/generateDiary.js` 的行為？
- [ ] 修改是否**沒有**影響其他書籍（`AI_TAO`, `GameVictory`, `mirror-realm`）的渲染？

### ✅ 3. 連結與依賴檢查 (Links & Dependencies)

- [ ] 更新後是否有遺漏任何**內部連結**？（特別是 epub 下載連結、章節交叉引用）
- [ ] 更新後是否有遺漏任何**圖片引用**？（檢查 markdown 中的 `![](...)` 路徑）
- [ ] 如果搬移了檔案，是否**全站搜索並更新**了所有引用該檔案的路徑？
- [ ] 新增的外部依賴（npm packages）是否有在 `package.json` 中正確聲明？

### ✅ 4. i18n 同步檢查

- [ ] 如果修改了 `docs/` 中的文章結構，`i18n/en/` 和 `i18n/zh-HK/` 的鏡像結構是否同步更新？
- [ ] 如果修改了 `docusaurus.config.js` 中的 navbar 標籤，翻譯檔（`code.json`）是否需要更新？
- [ ] 如果新增了 `src/pages/` 中的頁面，是否需要為其他語系建立對應翻譯？

### ✅ 5. 回歸驗證 (Regression)

- [ ] 修改完成後，是否執行了 `npm run build` 確認建置成功？
- [ ] 建置日誌中是否有**新的 warning**（尤其是 `broken link` 或 `broken markdown link`）？
- [ ] 首頁 (`src/pages/index.tsx`) 是否仍正常渲染？

---

## 任務專屬檢查清單

### 📝 文章類修改（新增/更新 docs）

- [ ] Frontmatter 是否完整？（必填：`title`；建議填：`date`, `section`, `topic`, `summary`）
- [ ] 如果是新書籍章節，是否已在 `sidebars.js` 中手動加入？
- [ ] 如果是 TechNotes 或 Diary，是否使用 `autogenerated`（無需手動修改 sidebar）？
- [ ] 圖片是否放在正確位置？（書籍：`docs/<BookName>/`，TechNotes：`docs/TechNotes/images/`）
- [ ] 圖片是否已壓縮？（建議 <2MB/張）

### 🎨 UI 類修改（元件/樣式/版面）

- [ ] 樣式是否在 CSS Modules 中完成？
- [ ] 元件是否放在 `src/components/<ComponentName>/` 下？
- [ ] 是否影響了首頁的渲染？
- [ ] 是否需要更新 `src/css/custom.css` 中的 CSS 變數？（慎重操作）

### 🧩 新模組（Forum / AIGame / 其他）

- [ ] 頁面是否放在 `src/pages/<module-name>/`？
- [ ] 元件是否放在 `src/components/<ModuleName>/`？
- [ ] 靜態資源是否放在 `static/<module-name>/`？
- [ ] 路由是否與現有 `/docs/*` 路由互不衝突？
- [ ] 是否在 `docusaurus.config.js` 的 `navbar` 中新增了導覽項目？
- [ ] 是否遵循了 `architecture.md` 第 6 節的擴充守則？

### 📦 資產管理（搬移/刪除/重整檔案）

- [ ] 搬移前是否先全站搜索該檔名，列出所有引用？
- [ ] 搬移後是否更新了所有引用路徑？
- [ ] 刪除前是否確認該檔案真的沒有被任何 markdown 或元件引用？
- [ ] 靜態資源（epub、圖片）是否放在 `static/` 下正確的子目錄？

---

## 常見地雷

> ⛔ **不要做的事**
>
> 1. **不要直接修改 `src/data/*.json`**——這些是建置時自動生成的，修改會被覆蓋。
> 2. **不要在 `docs/TechNotes/` 或 `docs/Diary/` 修改 sidebar 結構**——它們使用 `autogenerated`，sidebar 由 frontmatter 的 `sidebar_position` 控制。
> 3. **不要在全域 `custom.css` 中加模組專屬樣式**——使用 CSS Modules。
> 4. **不要把新模組的內容放進 `docs/`**——除非它是文章/書籍性質的內容。
> 5. **不要忘記 epub 已被 `.gitignore` 排除**——epub 搬移後需確認不會被 git 追蹤。

> 💡 **建議做的事**
>
> 1. 修改完成後，跑一次 `npm run build`。
> 2. 搬移圖片前，先用 `grep -r "filename"` 確認引用。
> 3. 大型圖片先壓縮再提交（推薦 TinyPNG 或 `scratch/compress_images.py`）。
> 4. 每次修改後更新 `architecture.md` 的「最後更新」日期。

---

## 自我檢查報告模板

每次完成修改後，AI 助手應在回覆中附上以下格式的自我檢查報告：

```markdown
### 🔍 自我檢查報告

| 檢查項目 | 結果 | 備註 |
|---|---|---|
| RWD 影響 | ✅ 無影響 / ⚠️ 已處理 | — |
| 模組隔離 | ✅ 無汙染 | — |
| 連結完整性 | ✅ 已驗證 | — |
| i18n 同步 | ✅ 無需更新 / ⚠️ 已同步 | — |
| 建置驗證 | ✅ `npm run build` 通過 | — |
```
